<!DOCTYPE html>
<html>
<head>
    <title>Caja de regalo con mensaje oculto</title>
    <style>
        .gift-box {
            width: 200px;
            height: 200px;
            background-color: #f5f5f5;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            perspective: 1000px;
        }

        .gift-box .lid {
            width: 100%;
            height: 50%;
            background-color: #f5f5f5;
            transform: rotateX(0deg);
            transition: transform 0.5s ease-in-out;
        }

        .gift-box .message {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 24px;
            font-weight: bold;
            color: #ff0000;
            text-align: center;
            visibility: hidden;
        }

        .gift-box:hover .lid {
            transform: rotateX(90deg);
        }

        .gift-box:hover .message {
            visibility: visible;
        }
    </style>
</head>
<body>
    <div class="gift-box">
        <div class="lid"></div>
        <div class="message">Te odio</div>
    </div>
</body>
</html>
